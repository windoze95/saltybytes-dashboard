services:
  dashboard:
    build: .
    ports:
      - "80:80"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - DASHBOARD_PASSWORD=${DASHBOARD_PASSWORD}
      - PORT=80
      - DATA_DIR=/data
    volumes:
      - dashboard-data:/data
    restart: unless-stopped

volumes:
  dashboard-data:
